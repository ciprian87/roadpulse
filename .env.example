# Database — credentials must match DB_USER/DB_PASSWORD below
# Generate DB_PASSWORD with: openssl rand -hex 32
DB_USER=roadpulse
DB_PASSWORD=
DB_NAME=roadpulse
DATABASE_URL=postgresql://roadpulse:CHANGE_ME@localhost:5432/roadpulse
# Redis — password must match REDIS_PASSWORD below
# Generate REDIS_PASSWORD with: openssl rand -hex 32
REDIS_PASSWORD=
REDIS_URL=redis://:CHANGE_ME@localhost:6379
NEXTAUTH_SECRET=change-me-in-production
NEXTAUTH_URL=http://localhost:3000
OPENROUTESERVICE_API_KEY=
NWS_USER_AGENT=RoadPulse/1.0 (contact@roadpulse.app)
# Generate with: openssl rand -hex 32
CRON_SECRET=
# Generate with: openssl rand -hex 32
HEALTH_TOKEN=
